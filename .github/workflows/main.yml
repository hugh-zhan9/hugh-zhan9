name: Waka Readme
 
on:
  push:
    branches:
      - master
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"
 
jobs:
  update-readme:
    name: Update this repo's README 
    runs-on: ubuntu-latest
    steps:
      - uses: hugh-zhan9/hugh-zhan9@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
